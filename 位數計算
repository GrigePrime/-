#include <stdio.h>

int fun(int n){
    if(n < 10){
        return 1;
    }else{
       return 1 + fun((int) n / 10);
    }
}
int main(void){
        unsigned long long int m ;
        scanf("%lu",&m);
        printf("%d\n",fun(m));
    }
